apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otus-dz2
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  rules:
  - host: arch.homework
    http:
      paths:
      - path: /health(/|$)(.*)
        pathType: Prefix
        backend:
          service:
            name: otus-dz2
            port:
              number: 80